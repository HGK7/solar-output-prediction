# =============================================================
# Solar API — production dependencies (Render free tier)
# =============================================================

# --- Web Framework ---
flask>=3.1,<4.0
flask-cors>=5.0,<6.0
gunicorn>=23.0,<24.0

# --- ML / Data (inference + build-time training) ---
numpy>=2.1,<3.0
pandas>=2.2,<3.0
scikit-learn>=1.6,<2.0
joblib>=1.4,<2.0

# --- LLM (explanation only; lazy if key missing) ---
google-genai>=1.0,<2.0
langchain-google-genai>=4.2,<5.0

# --- RAG (lazy-loaded on /ask or when ENABLE_RAG_GROUNDING=true) ---
chromadb>=1.3.5,<2.0
sentence-transformers>=3.3,<4.0
langchain-chroma>=1.0,<2.0
langchain-huggingface>=1.0,<2.0

# --- HTTP & Caching ---
requests>=2.32,<3.0
cachetools>=5.5,<6.0

# --- Utilities ---
python-dotenv>=1.0,<2.0

# PySAM, shap, langgraph, mcp → see requirements-dev.txt (local / full profile)
