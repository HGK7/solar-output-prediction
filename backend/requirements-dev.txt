# Optional dependencies — local dev, ingest scripts, full physics profile
-r requirements.txt

# --- Physics (heavy — set PHYSICS_MODE=pysam) ---
NREL-PySAM>=5.1,<6.0

# --- Unused in runtime; kept for experiments / future work ---
shap>=0.50,<1.0
langgraph>=1.1,<2.0
mcp>=1.2,<2.0
langchain>=1.2,<2.0
langchain-community>=0.3,<0.4
langchain-text-splitters>=0.3,<0.4

# --- Document ingest (scripts/ingest_science_docs.py) ---
pypdf>=5.0,<6.0
